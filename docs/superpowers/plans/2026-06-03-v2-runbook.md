# V2 Production Run — Runbook

> **Data status (verified 2026-06-12):** collection is FINISHED. All 16 streams span
> 2026-03-14 12:48 → 2026-06-10 13:57 UTC (≈88.0 days); collector container removed,
> raw hypertable and lob_5s aggregate agree on the end timestamp.

1. **Rotate leaked Binance keys FIRST** (independent of this work): the live key+secret
   sit in plaintext in `executor/config.json` + `executor/.env`. Verified 2026-06-12: these
   files were **never committed** (`git log --all -- executor/` is empty; only `.env.example`
   is tracked) — so no history scrub is needed, but the keys ran live and must still be
   rotated/revoked on Binance and kept untracked.
2. **Fresh export** (on the DB host):
   `python export_training_data.py --fresh`  → verify ~1.4–1.5M rows/stream; `lob_training_data.zip` regenerated.
   - **Expected duration ≈ 1.5 h** (measured 2026-06-04 under live collector load; collector is now
     stopped so it may run faster). ~458+ MB parquet per stream → **~7+ GB total**.
   - The export sets `max_parallel_workers_per_gather = 0` (validated): parallel workers allocate
     dynamic shared memory in the container's small `/dev/shm` (256 MB per docker-compose), which
     fails the COPY with *"could not resize shared memory segment"*. Serial plan avoids it.
3. **Code snapshot**: `git archive --format=zip -o aai_code.zip HEAD -- training/`
   — required because local `main` is unpushed (origin/main lacks the entire V2 pipeline; it has
   also diverged 75/24 commits, so do NOT push blindly).
4. **Upload** BOTH `lob_training_data.zip` and `aai_code.zip` to Google Drive folder
   `training data` (overwrite).
5. **Colab**: open a High-RAM A100 runtime and run cells 1–6 from `training/colab_v2.md`:
   - Cell 4 trains the **V1 baseline on the A100** (`train.py --source parquet --savgol-window 11`)
     and re-measures its directional accuracy (`baseline_v1.py --source parquet`). Savgol 11 matches
     the V2 pipeline so the comparison is apples-to-apples (plan Task 12). Training V1 locally
     against the DB is no longer needed.
   - Cell 5 runs V2 train+eval+backtest (`colab_v2_runner.py`). Use `--resume` if the session drops.
6. **Read** `experiments/<run>/eval/report.json` (per-horizon accuracy vs V1 baseline; ECE before/after;
   confidence-coverage) and `experiments/<run>/backtest/report.json` (maker/taker net PnL, conf→PnL curve).
7. **Decision gate:** proceed to execution only if calibrated high-confidence accuracy clears the
   V1 baseline AND maker-cost net PnL is positive at a usable coverage.
